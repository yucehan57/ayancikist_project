# from django import forms
# from userprofile.models import UserProfile
#
# class UserProfileForm(forms.ModelForm):
#     class Meta:
#         model = UserProfile
#         # exclude slug field on 'UserProfile' model since it is
#         # automatically assigned by user's username --making it a unique
#         # slug. A good use for Profile Page
#         exclude = ('user', 'slug')
